<Window x:Class="Ressources.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ressources"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{Binding UserModelData.Nom}"/>
            <TextBlock Text="{Binding UserModelData.Prenom}"/>
            <TextBlock Text="{Binding UserModelData.DateNaissance}"/>
            <TextBlock Text="{Binding UserModelData.Age}"/>
        </StackPanel>
    </Grid>
</Window>
